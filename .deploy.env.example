# IPhey Deployment Credentials Template
# ⚠️  Copy this file to .deploy.env and fill in your actual values
# ⚠️  .deploy.env is gitignored and will never be committed to Git

# ============================================================================
# Cloudflare Credentials
# ============================================================================

# Cloudflare API Token
# Generate at: https://dash.cloudflare.com/profile/api-tokens
# Use template: "Edit Cloudflare Workers" or create custom token with:
#   - Account.Cloudflare Workers Scripts: Edit
#   - Account.Cloudflare Pages: Edit
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token_here

# Cloudflare Account ID
# Find at: https://dash.cloudflare.com/ → Select account → Copy ID from sidebar
CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id_here

# Cloudflare Radar API Token (optional - defaults to CLOUDFLARE_API_TOKEN)
# Same as CLOUDFLARE_API_TOKEN or create a separate token
CLOUDFLARE_RADAR_TOKEN=

# ============================================================================
# Third-Party API Keys (Required for Worker)
# ============================================================================

# IPInfo.io API Token
# Sign up at: https://ipinfo.io/signup
# Free tier: 50,000 requests/month
IPINFO_TOKEN=your_ipinfo_token_here

# AbuseIPDB API Key
# Sign up at: https://www.abuseipdb.com/
# Dashboard → API → Copy API Key
ABUSEIPDB_API_KEY=your_abuseipdb_api_key_here

# ============================================================================
# Production Configuration
# ============================================================================

# Production API URL (optional - defaults to https://iphey-api.difft.workers.dev)
# This is the URL of your deployed Cloudflare Worker
NEXT_PUBLIC_API_URL=https://iphey-api.difft.workers.dev

# ============================================================================
# GitHub Token (Optional - only needed if running setup script)
# ============================================================================

# GitHub Personal Access Token (only for automated setup via script)
# Generate at: https://github.com/settings/tokens
# Required scopes: repo, workflow
# Note: Not needed if using GitHub CLI (gh auth login)
GITHUB_TOKEN=

# ============================================================================
# Notes
# ============================================================================
# 1. All values are required except where marked optional
# 2. GITHUB_TOKEN is only needed if NOT using GitHub CLI
# 3. Run setup script: ./scripts/setup-deployment.sh
# 4. Or manually set secrets: gh secret set SECRET_NAME --body "value"

